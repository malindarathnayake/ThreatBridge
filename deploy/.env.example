# =============================================================================
# ThreatBridge Environment Variables (Optional)
# =============================================================================
# Copy to .env and customize as needed
# Most settings have sensible defaults - only set what you need to override

# API Configuration
# API_PORT=8000
# LOG_LEVEL=INFO

# Feed URLs (for feeds using "from env var:" syntax)
# Add URLs for any paid or authenticated feeds you want to use
# MALWAREURL_FEED_URL=https://www.malwareurl.com/your-feed-url
# PROOFPOINT_FEED_URL=
# ABUSEIPDB_FEED_URL=
# OTX_FEED_URL=

# Note: Many feeds work without API keys and are configured directly in feeds.yml
# Only create this .env file if you need to override defaults or add custom feed URLs
